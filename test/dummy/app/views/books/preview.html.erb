<h1>Preview: <%= crud_label(@book) %></h1>
<p class="text-muted">A custom member action (<code>action :preview, icon: 'eye'</code>) —
its path was derived from the conventional <code>preview_book_path</code> route.</p>
<%= crud_record @book, fieldset: :compact, actions: false %>
