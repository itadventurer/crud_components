<h1>Import books</h1>
<p class="text-muted">A custom <strong>collection</strong> action
(<code>action :import, on: :collection, icon: 'upload'</code>) — it appears as a
button in the table header, and its path derives from the conventional
<code>import_books_path</code> route.</p>
<%= link_to '← Back to books', books_path, class: 'btn btn-outline-secondary' %>
