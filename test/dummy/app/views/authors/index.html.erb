<h1>Authors</h1>
<p class="text-muted">This model has <strong>zero configuration</strong> — not even
<code>include CrudComponents::Model</code>. Everything below is derived.</p>
<div class="card"><div class="card-body">
  <%= crud_collection Author %>
</div></div>
