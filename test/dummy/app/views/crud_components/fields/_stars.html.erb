<%# The app-level custom renderer from the README: a partial at the
    conventional path + `attribute :rating, as: :stars`. %>
<span class="text-warning" title="<%= value %>/5"><%= '★' * value.to_i %><span class="text-muted"><%= '☆' * (5 - value.to_i) %></span></span>
