<%# Locals: value (whether a value is stored), record, field, surface.
    A secret is never shown, only whether there is one. %>
<%- if value -%>
  <span class="<%= CrudComponents.config.css.muted %>"><%= t('crud_components.secret.set', default: 'Set') %></span>
<%- else -%>
  <span class="<%= CrudComponents.config.css.muted %>">—</span>
<%- end -%>
